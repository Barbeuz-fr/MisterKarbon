<div class="container">
  <h1>Liste des articles</h1>
  <% @articles.each do |article| %>
    <div>
      <h3><%= link_to article.title, article_path(article), class:""  %></h3>
      <p><%= link_to "Edit", edit_article_path(article) %></p>
      <p><%= link_to "Delete", article_path(article), method: :delete %></p>
    </div>
  <% end %>
  <br>
  <div>
    <h1><%= link_to "Ajouter un article", new_article_path, class:"btn btn-primary"  %></h1>
  </div>
  <h1><%= link_to "Retour à la page d'accueil", root_path, class:"btn btn-light"  %></h1>
</div>
