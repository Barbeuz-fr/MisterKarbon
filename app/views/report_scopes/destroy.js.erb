function refreshCard(innerHTML) {
  const card = document.getElementById("<%= @report_scope.emission_module.id %>");
  card.outerHTML = innerHTML;
}
function deleteCard(innerHTML) {
// trouver la carte correspondante ds la div selected module
// la supprimer .outerHTML = ''
}
refreshCard('<%= j render "report-scope-create", module_scope: @report_scope.emission_module, report: @report, report_scope: ReportScope.new %>')
