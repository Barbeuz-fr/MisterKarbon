<div class="banner-contact"
     style="background-image: linear-gradient(rgba(255,255,255,0.7),rgba(255,255,255,0.8)),
            url(https://res.cloudinary.com/dkybxjjy2/image/upload/v1586855718/KC-homepage-banner_dodnwh.png);">
  <div class="container">
    <div class="row">
      <div class="col-3">
      </div>
      <div class="col-6">
        <div class="pt-1">
          <h5>Des questions? Envie d'une démo? <br>Dites-nous tout!</h5>
          <%= form_for(@contact) do |f| %>
            <div class="field mb-3 d-flex justify-content-between">
              <%= f.text_field :name, placeholder: "Votre nom", style: 'width:35%;' %>
              <%= f.text_field :email, placeholder: "Votre email", style: 'width:60%' %>
            </div>
            <div class="field mb-3">
              <%= f.text_area :message, placeholder: "Votre message", style: 'width:100%; height: 20vh;'  %>
            </div>
            <div class="field mb-3">
              <%= f.label "M'abonner à la newsletter" %>
              <%= check_box_tag(:newsletter) %>
            </div>
            <div class="d-flex justify-content-between">
              <%= link_to "Retour à la page d'accueil", root_path, class: "btn btn-light"%>
              <div class="actions">
                <%= f.submit "Envoyer", class: 'btn btn-primary'%>
              </div>
            </div>
          <% end %>
          <br>
        </div>
      </div>
      <div class="col-3">
      </div>
    </div>
  </div>
</div>


